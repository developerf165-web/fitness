const MailingFormHeader = ({ title }) => {
  return (
    <div className="flex-shrink-0 pb-4 pt-4">
      <h2 className="text-white text-2xl font-bold text-center">{title}</h2>
    </div>
  );
};

export default MailingFormHeader;